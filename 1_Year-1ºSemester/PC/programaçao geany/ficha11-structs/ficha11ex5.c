#include <stdio.h>
#include <math.h>

typedef struct {
	int R; 
	double Area; 
	double Comp; 
}Circ;
const pi = 3.14;

float area_c(R)
{
	float result;
	result=pi*(R*R);
	return(result);
}
float perim_c(R)
{
	float result;
	result=2*pi*R;
	return(result);
}

struct CIRC Func_atributos(int raio);
	
	void 

int main()

Circ circulo;

